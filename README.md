# Pusti Pure CRM

A React + Vite CRM dashboard prototype built with Tailwind CSS and Lucide icons.

## Setup

1. Run `npm install`
2. Run `npm run dev`

## Available Scripts

- `npm run dev` — start the development server
- `npm run build` — build the app for production
- `npm run preview` — preview the production build
